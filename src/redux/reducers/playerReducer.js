const initialState ={
    currentTrack = null,
};

const playerReducer = (state = initialState, action) =>{

switch (action.type){
    case SET_CURRENT_TRACK:
        return{
            
        }
}
}